FOR IF-STATEMENTS
1. The AST should generate the IR code.
2. Labels for if-statements and else-statments should be handled in the AST node.
3. Optimize code: use constant propagation to replace variables in if-statements with actual values; handle OR statments like if one side is true ignore the other side.

FOR GENERAL IMPROVEMENTS
1. Calculate efficiency of the compiler. (business practice).
2. Type coercion (computing expressions in a certain way).
3. Type casting.
4. Print errors.