I have a new task for a class I am in. This is theoretical so it does not need to work, but the task it to implement a new compiler feature and showcase it in class. I have a compiler working in an attached folder, but the new feature I want to add is a "when" loop. The idea is that a block of code attached to the when loops until the when is true, but or's and else's can also be executed. It would look something like.
#file:for in-class activity

x = 7
when (x > 10)
{

}
or (x < 8)
{
break when (x = 6);
}
else
{
x++;
}

Given the following steps, can you help me impelement it, as well as when doing so add comments to the added code to differentiate where the new feature code is added? If code needed already exists or is reused you dont need to add comments.

Steps:
1. Add lexer tokens
Add tokens for WHEN, OR (if not same as existing, maybe the keyword should be WHENOR), and BREAK-WHEN syntax (BREAK and WHEN already separated).
Update scanner.l to recognize WHEN and OR keywords (and BREAK if missing).

2. Extend parser grammar (parser.y)
Add grammar rules for when_stmt, branch syntax (OR '(' expr ')' block), optional ELSE block, and break_when_stmt.
Integrate when_stmt as a valid statement where other statements are accepted.
Ensure precedence and conflict resolution (shift/reduce) are considered; test with yacc/bison verbose output.

3. Add AST node kinds (ast.h / ast.c)
Add AST_WHEN_STMT, AST_WHEN_BRANCH, AST_BREAK_WHEN (or similar).
Define structures: AstWhen (primary cond, list of branches, optional else block), AstWhenBranch (cond + block).
Implement constructors/factories for new AST nodes and free routines.

4. Build parser -> AST actions (parser.y)
In parser reductions for when_stmt and break_when_stmt, create the new AST nodes.
Build a linked list or vector for OR branches in the AST.

5. Update semantic analysis / type checking
If you have a semantic pass, validate that branch conditions are expressions (boolean-compatible).
Ensure break_when expression is valid (if it contains assignment, verify allowed).
Add error messages for malformed when constructs.

6. Plan lowering strategy to TAC/IR
Choose lowering model: (A) expand into labels + conditional jumps (recommended), or (B) implement a new TAC opcode.
Define label naming scheme and how the done_label for each when is tracked.

7. Modify codegen/tac generation (tac.c / codegen.c)
Add routine gen_ast_when or lower_when that:
emits start_label
evaluates W; if W true -> goto done_label
for each OR branch: evaluate its cond; if true -> goto branch_label
if none matched -> run else (or fall back to loop restart)
after each branch block, emit goto start_label
emit done_label after loop
Implement break-when lowering: evaluate expression, then emit goto done_label.

8. Implement label scoping / stack
Add a stack in codegen state for current_when_done_label so nested when loops work and break-when knows target.
Push done_label before lowering body; pop after done.

9. Handle control-flow fall-throughs
Ensure that branch blocks end with an unconditional jump back to start_label (unless they themselves contain a break when that jumps to done_label).
Avoid unintended fall-through to subsequent code.

10. Update TAC emission utilities
Add helpers to emit conditional jump on expression results, emit label, emit goto.
Reuse existing if possible.

11. Update any optimizer/passes
If you have passes that expect specific statement kinds, add handling for AST_WHEN_STMT or lowered TAC form.
Ensure dead-code elimination / CFG builders account for new labels and jumps.

12. Update symbol table / scopes if needed
If when introduces its own scope for variables declared in branch blocks, ensure symbol table pushes/pops accordingly.